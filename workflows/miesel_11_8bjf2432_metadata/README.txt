manually generated:
llm_generated_protocol_outline.yaml (also saved as manual_outline.yaml so it won't accidentally get overwritten)
llm_biosample_raw_file_mapper.csv (also saved as manual_mapper.csv so it won't accidentally get overwritten)

This was done to include all workflows in one material processing metadata generation, since the processed samples are related

